#include "sort.h"

/**
 * quick_sort - use quick sort algorithm
 * @array: pointer to the array
 * @size: of the array
 * Return: end program
 **/
void quick_sort(int *array, size_t size)
{
	if (!array || size < 2)
		return;
}
